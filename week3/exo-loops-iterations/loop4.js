/* Avec une boucle for-of afficher les éléments du tableau suivant, ligne par ligne:

let tab = ['Js', 'Solidity', 'Html', 'Css', 'Python', 'Java', 'C++', 'Rust'] */

let tab = ['Js', 'Solidity', 'Html', 'Css', 'Python', 'Java', 'C++', 'Rust']

for (const elem of tab) {
    console.log(elem)
  }

  // entries à privilégier / Extraire les données d'un array et les assignent à ses variables / [0, Js], [1, Solidity] etc
  /* for (const elem of tab.entries()) {
    console.log(elem)
  } */